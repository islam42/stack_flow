<% provide(:title, 'Edit')%>

  <div class="continer-fluid">
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <h2 class="center">Edit <%= resource_name.to_s.humanize %></h2>

      <%= render partial: 'form', locals: { btn_text: 'Update' } %>

    </div>
  </div>
</div>